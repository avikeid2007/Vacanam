<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:tb="http://www.hardcodet.net/taskbar">

    <!-- ═══════════════════════════════════════════════════════════════════════
         Vacanam System Tray Icon & Context Menu (Windows 11 Dark Fluent Theme)
    ════════════════════════════════════════════════════════════════════════════ -->

    <!-- Custom ContextMenu Style: Removes default WPF white icon gutter column -->
    <Style x:Key="DarkContextMenuStyle" TargetType="{x:Type ContextMenu}">
        <Setter Property="Background" Value="#12121A"/>
        <Setter Property="BorderBrush" Value="#2B2B40"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Grid.IsSharedSizeScope" Value="True"/>
        <Setter Property="HasDropShadow" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ContextMenu}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="12"
                            Padding="6">
                        <Border.Effect>
                            <DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="4" Opacity="0.6"/>
                        </Border.Effect>
                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle"
                                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Custom MenuItem Style: Clean dark hover state without icon column -->
    <Style x:Key="DarkMenuItemStyle" TargetType="{x:Type MenuItem}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="#F1F1F8"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI, sans-serif"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="Margin" Value="0,1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type MenuItem}">
                    <Border x:Name="ItemBorder"
                            Background="{TemplateBinding Background}"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter x:Name="HeaderHost"
                                          ContentSource="Header"
                                          RecognizesAccessKey="True"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="ItemBorder" Property="Background" Value="#222238"/>
                            <Setter Property="Foreground" Value="#FFFFFF"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="#525270"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Custom Separator Style -->
    <Style x:Key="DarkMenuSeparatorStyle" TargetType="{x:Type Separator}">
        <Setter Property="Background" Value="#252538"/>
        <Setter Property="Margin" Value="6,4"/>
        <Setter Property="Height" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Separator}">
                    <Border Height="{TemplateBinding Height}"
                            Background="{TemplateBinding Background}"
                            Margin="{TemplateBinding Margin}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <tb:TaskbarIcon x:Key="VacanamTrayIcon"
                    x:Shared="false"
                    IconSource="/Resources/Icons/vacanam.ico"
                    ToolTipText="Vacanam — Ready (Ctrl+Space to record)"
                    LeftClickCommand="{Binding OpenSettingsCommand}"
                    Visibility="Visible">

        <tb:TaskbarIcon.ContextMenu>
            <ContextMenu Style="{StaticResource DarkContextMenuStyle}">

                <!-- App Header Branding Row -->
                <MenuItem Style="{StaticResource DarkMenuItemStyle}" IsEnabled="False" Focusable="False">
                    <MenuItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Vacanam"
                                       Foreground="#6366F1"
                                       FontWeight="Bold"
                                       FontSize="14"
                                       VerticalAlignment="Center"/>
                            <TextBlock Text=" — Ready"
                                       Foreground="#10B981"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </MenuItem.Header>
                </MenuItem>

                <Separator Style="{StaticResource DarkMenuSeparatorStyle}"/>

                <!-- Start / Stop Recording (toggle) -->
                <MenuItem Header="▶  Start Dictation"
                          Style="{StaticResource DarkMenuItemStyle}"
                          Command="{Binding ToggleRecordingCommand}">
                    <MenuItem.ToolTip>
                        <ToolTip Content="Click to start recording. Click again to stop. Or hold Ctrl+Space."/>
                    </MenuItem.ToolTip>
                </MenuItem>

                <Separator Style="{StaticResource DarkMenuSeparatorStyle}"/>

                <!-- Settings -->
                <MenuItem Header="⚙  Settings"
                          Style="{StaticResource DarkMenuItemStyle}"
                          Command="{Binding OpenSettingsCommand}"/>

                <!-- History -->
                <MenuItem Header="📜  Transcript History"
                          Style="{StaticResource DarkMenuItemStyle}"
                          Command="{Binding OpenSettingsCommand}"/>

                <!-- Audio Settings -->
                <MenuItem Header="🎙  Microphone &amp; Audio"
                          Style="{StaticResource DarkMenuItemStyle}"
                          Command="{Binding OpenSettingsCommand}"/>

                <!-- Models -->
                <MenuItem Header="🤖  Speech &amp; AI Models"
                          Style="{StaticResource DarkMenuItemStyle}"
                          Command="{Binding OpenSettingsCommand}"/>

                <Separator Style="{StaticResource DarkMenuSeparatorStyle}"/>

                <!-- Exit -->
                <MenuItem Header="✕  Exit"
                          Style="{StaticResource DarkMenuItemStyle}"
                          Foreground="#EF4444"
                          Command="{Binding ExitApplicationCommand}"/>

            </ContextMenu>
        </tb:TaskbarIcon.ContextMenu>

    </tb:TaskbarIcon>

</ResourceDictionary>
